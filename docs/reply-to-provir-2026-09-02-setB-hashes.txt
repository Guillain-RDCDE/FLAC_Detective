Jamie,

Set B arrived and it is intact. Here are my two hashes. They are the thing I
owe you before either key moves.

    verdicts_setB_v1136.csv   376bcc89436cfeece6bd8b58c7112e0a1df580bea85498fb978a8b9051c1df91
    verdicts_setB_v1134.csv   7e1abf9212137718a4fb6d96199df72462192b0a56a70fe4a7e4d91e056989e3

That is my half committed. Send your key in your next message and mine goes in
the same message back, exactly as the method says. Nothing below changes those
two numbers; they were fixed before I wrote a word of this.


WHAT I VERIFIED ON ARRIVAL

Zip 2,963,597,744 bytes, sha256 f53cc861…0ceffb, matching SHA256SUMS.txt. All
282 entries stored, none compressed, so the bytes inside are the bytes the
manifest describes, as you said.

Manifest self-hash b0bb5410…46eaea, identical to what you declared on the 31st.
Worth one line because it bit us both before: the manifest itself is LF and
hashes as it stands, while MANIFEST.selfhash carries the bare hex with CRLF —
the same convention as your .sha256. Two files, two line-ending rules, and both
matter.

All 280 files match the manifest on hash AND on the declared size column. 280
unique names, nothing missing, nothing extra. Verified at read time, from inside
the zip and again from the extracted copy: my runner refuses to score a set it
has not hashed itself.

Your length leak is closed. All 280 are exactly 10,584,078 bytes — 2,646,000
samples, one single value, 44.1 kHz / 16-bit stereo throughout.

One thing you may not have checked yourself: the WAV headers carry a LIST/INFO
chunk with an encoder string, and it is byte-identical on all 280. So it leaks
nothing — it does not separate your genuine class from your atrac3plus arm. I
mention it because if it had varied by class, it would have handed me the answer
key, and neither of us would have noticed until it was too late to matter.


THE MISSING LINK, CLOSED

Your setA_r2_verdicts.csv.sha256 hashes c4dceb27…ab1c28. That is bit-for-bit
what I reconstructed by brute force. The .ots you sent is byte-identical to the
one I already held, it seals the CRLF variant — the LF digest 3e6d4301… does not
appear anywhere in the binary — and it carries four calendar URLs: bob and alice
at opentimestamps, catallaxy, eternitywall.

So the chain now closes without reconstruction: CSV 8c1f04d7… -> hash file
c4dceb27… -> proof. A third party no longer has to guess a file format to check
your work. Thank you for sending it; the difference between "verifiable" and
"verifiable if you also happen to guess CRLF" is the whole of it.


MY TWO COLUMNS, AND A NULL I DID NOT TRUST

Both files: 280 rows, 280 unique ids, 0 ERROR, 0 NOT_ASSESSED, longest
contiguous ERROR run 0. Version and commit stamped on every row — 1.13.6 at
55c4aeb8, 1.13.4 at 9199d452.

The two columns are identical on all 280 rows, verdict for verdict. The
band-limited independence guard (1.13.5) and NOT_ASSESSED (1.13.6) change
nothing on your set. That is exactly what the second column was for, and your
half is the only corpus neither of us built, so it is the only honest place to
have asked.

But a perfect zero is also what a broken harness looks like — two passes that
silently loaded the same engine. So I did not report it until I had a positive
control, registered before it ran: the four band-limited files that my recorded
1.13.5 measurement says MUST move. They come back 4/4 FAKE_CERTAIN under 1.13.4
and 4/4 SUSPICIOUS under 1.13.6, with the log printing "conviction withheld:
score 105 but only 1 evidence family (cnn&spectral)". Today's chain separates
the two engines perfectly. The null on your set is a measurement, not an
artefact.

Two traps found on the way there, both mine, both worth your time because they
are the species we keep finding in each other's work:

My virtual environment carries an editable install pointing at the repository.
Set the path wrong and both columns quietly become the same engine while still
printing two different version numbers — because the version string comes from
whichever module actually loaded. The check that works is the module's file
path, not its version.

And my first attempt at diffing the two engine passes joined on filename alone.
Filenames repeat across arms, so it compared one arm against another and
reported 60-odd changes where there were four. Composite key, or no key at all.


A DISCLOSURE I OWE YOU, SAME SPECIES AS YOURS

The frame-count audit in the freezer that produced set A r2 — the one I
described to you as "a check in the freezer rather than a check in my
attention" — logged and prevented nothing. It set a failure flag into a variable
nothing ever read, and the audit itself sat in a branch that could be skipped.

Set A r2 is good: all 288 files were truncated to 2,646,000 samples and I
verified that by hand. But the guarantee rests on that manual check, not on the
automatic one I told you about. I found it by sweeping for the pattern two hours
after reading your account of the inert timestamp client. Same species, one day
apart: yours printed into a console nobody watched, mine wrote into a variable
nobody read. It is repaired, with five tests, two of which fail against the
previous version.

That is now a standing rule on my side, in your words as much as mine: after any
repair that adds a check, grep for who reads its result.


THREE THINGS FROM MY SIDE THAT ARE NOT ABOUT YOUR SET

The constant that mattered was 16,000 Hz, not 17,000. Taking the symmetric value
from the neighbouring guard would have destroyed three TRUE convictions on
128 kbps material — a low-bitrate transcode and a genuinely band-limited
transfer both live at 15-16 kHz. It was invisible until I built the low-rate
arms BEFORE choosing, because every arm in set A is 256 kbps or better and at
that rate 17,000 shows zero cost. A constant priced only on the material you
shipped is not priced at all.

Your family-lock idea works, but one layer down. With a probe-calibrated null
and an abstention rule, the five predictions hold on a held-out set. It was
wrong at the layer we first discussed it and right at the next one, which is
not the same as being wrong.

Generation counting is closed. Twenty-four times more phases changed nothing —
identical AUC to three decimals, and the search picked phase 0 on 96 readings
out of 96. It is a population statistic, definitively, and I am not reopening
it.

And a fourth, because it went against me: a vinyl rotation probe. 33-1/3 rpm is
0.5556 Hz, a physical clock inside the recording, so it should say whether a
file is being replayed at its capture rate. It passes on constructed cases —
inject 0.5556, it finds 0.5556. On a real documented 24/96 vinyl transfer it
returns 19.5 to 67.5 "rpm" with no clustering: the musical phrasing buries the
eccentricity. A second formulation failed the same way. Recorded as refused. I
am telling you because I nearly stopped at the constructed case, which would
have handed you a probe that works only on my own examples.


YOUR MEASUREMENT, AND WHAT I DO WITH IT

Accepted, and it settles the ordering question. 5/5 before the encode on both
filters, 0-1/5 after, and 0/5 on lossless with no encode at all — so neither
filter manufactures a reading, and what fires is the encoder's own structure.
Your kill condition was registered in advance and was not met. A filter in front
of the encoder is simply different music to the encoder; only something applied
afterwards can smear what it laid down.

So a silent Vorbis row on my half is evidence about the row, and I score it as a
miss with your declared exposure printed beside the number. Your n = 5 caveat is
yours and I will print it too: it answers the ordering, it is not a rate.

Thank you for not approximating my filter. 0.34 degrees RMS across four phase
figures, and group delay 5.72 -> 9.31 against my 5.7 -> 9.3, is the difference
between testing my filter and testing something like it.

One number back for your B5 clause: NOT_ASSESSED reads 0 on your 280 files, as
it read 0 on 1,248 real files here. The abstention vocabulary I added repairs no
observed failure, and I will keep saying so.


ORDER

Your key, my key, same message. Then I score both halves with the scorer
unmodified, half B first, then half A. Then, and only then, the organisation.

You are right that a pooled codebase is one witness with two names, and I am
glad it is in writing on both sides now. Two instruments built separately, on
different corpora, by people who disagree about method — that is the only thing
either of us has that survives being attacked on method, and method is where
it will be attacked first.

Your sentence is better than mine, so I will use yours: the public deserve to
know what they are paying for, and the platforms deserve to pay to prevent it
happening again. My hi-res files and your 192-as-WAV are the same fraud in
different units.

Still waiting on one thing only: your reading of the Teardrops arithmetic.
0.9187 against 44100/48000 = 0.918750 is five parts in a hundred thousand, and
your quartz-lock argument excludes the turntable but not what sits downstream of
the converter.

-- Guillain
