Jamie —

Taking your three in order, and then yes to the fourth.

Before anything else, your claim checks out and then some. You wrote that at least one of the 20004.0 / 0.0 files is one my key marks as fake. It is stronger than that: of the 21 rows reading exactly 20004.0, twenty are fake by the key outright, and the twenty-first is 0197 — the MP2 chain, fake on the taper's admission. Every single one of them is lossy in truth. Restricted to the eleven that also read std 0.0: ten fake by the key, plus 0197 again. So the miss is not free, you are right about that, and I am not going to argue the principle at all: an explicit is None in place of the truthiness test is the whole fix, exactly as you say, and a zero standard deviation is the strongest reading that column can produce, not the weakest.

The bug itself, though, I have to hand back before I can accept it. The guard you describe is not in my tree. opus_edge appears nowhere in FLAC Detective 1.13.0 except inside the archived copy of your own return CSV, where it is your telemetry and not my code; and rather than tell you that from a grep, I wrote the audit and ran it: ml/typed_absence_audit.py walks the AST of all 148 modules under src/ and ml/ and reports two shapes on any identifier whose last component names a scalar measurement — x or <number> and not x. Zero findings. The tripwire is validated against a control block before it is believed, because a probe that has never fired proves nothing: four must-fire lines including your exact (edge_std or 999) < 160, six must-not-fire lines including if not cutoffs (a collection, where empty really is absence) and if not running. Four of four caught, no false positives. The first version of that filter matched substrings anywhere in a name and returned eleven findings, every one of them a list, a bool or a module constant — the same defect it exists to catch, inside its own filter, which I mention because it is the third time this month one of us has found the species in the instrument rather than in the data.

So: which engine, which file, which line? If it is mine I will fix it in an hour, register the cost and re-run the affected numbers rather than quietly reissuing them. If it turns out to be yours, nothing about the above changes what I owe you — the principle is adopted either way, and here is what your v2 rows say about it, which I looked at before writing any of it. 296 of the 590 rows carry telemetry.opus_edge.edge_std at all; 27 of those read exactly 0.0; 21 rows read edge 20004.0, eleven of them with std 0.0 — and not one of the 21 is clear. Twelve are convicted, nine flagged. On this set the coercion costs you no verdict at all: everything it pushes out of the window is already carried by other flags. What it costs is the attribution — 0197 is convicted on BRICKWALL_20K and your MP3 conjunction at 19,500, not on the edge that actually sits dead centre. That is worth fixing for what the column will mean next time, not for what it did this time.

Two things fell out of those rows that are yours to use. The first is a correction against myself: I suspected the zeros were your fallback agreeing with itself rather than your windows agreeing with each other, because width_hz sits at your 1500.0 sentinel on 22 of the 27. I measured it — 81 % sentinel among the std-zero rows against 68 % among the rest, on n=27 — and that is not a result. Reporting it because I went looking for it, and it did not hold. The second I do not think is nothing: 20004.0 recurs exactly, twenty-one times, against 201 distinct edge values across 296 rows, and it recurs across six unrelated sources. Eight of the twenty-one come from a single source — TenD2005-07-16, on eight of its ten arms, mp3_320 and aacmf and opus and vorbis and the parent alike. On that item the 20 kHz edge is the mother's, inherited by every arm you measured, which is a corroboration of your own second point arriving from the other direction. But the recurrence of an exact constant across unrelated sources, with a zero standard deviation attached, reads to me like a cell boundary rather than an agreement. You know your instrument's grid and I do not. If 20004.0 is a bin edge in the 8192-point stage, then is None is still the right fix and edge_std == 0.0 still is not evidence of stability — and you would rather know that before you widen the window.

Either way the rule is registered here as yours as much as mine, stamped in the code where the species lives and enforced by that audit as a release check: an absence is typed, it is never a value, and never a falsy value. Test is None, never truthiness, because 0.0 and 0 and "" are readings. Three instances in one week across two engines — your width returning a magic 1500.0, my detect_cutoff returning Nyquist for three different conditions, your edge_std coercion — and only the third one was caught by anything other than a human reading code, which is why it is a script now.

Your two notes on my key. 0197 is adopted, and further than you offered it: MP2 is MPEG-1 Layer II, 32 subbands and no MDCT stage, so both engines are hunting a filterbank the file does not contain, and carrying it as a miss misprices both of us rather than one. It is recorded as a codec class neither engine covers — an admission of coverage, not of failure — in a dated amendment beside the original adjudication, which is not edited; the label stays fake on the taper's admission, and your conviction of it stays right. The v3 consequence is in the protocol below.

0469 does not move, and you did not ask it to. What is recorded is your reading beside mine, in the field that exists for exactly this and carries no evidential weight: your AAC_LATTICE at 69 %, your dead run at 126, my stereo witness at 5.8, and your own opus_edge at 21,530 with std 155.9 — which is to say the file that four instruments across two independent engines read as lossy is admitted by your stability gate with 4.1 Hz to spare. And because a prediction that is only remembered when it is right is not a prediction, it is registered: if provenance ever turns up for 0469, it lands lossy. If it turns up genuine, that row is the counter-example and I will quote it as one.

(1 of 2)
