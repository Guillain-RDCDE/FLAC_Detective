Jamie —

Your sample-rate question first, because you asked for a minute's work and it took one, and then the thing your letter cost me, which is the part worth having.

All twenty-one rows reading 20004.0 are 44,100 Hz. Every one, on both your sample_rate and your telemetry.sr. So it is not a rate you have not accounted for, and it is not a parse on my side either — I read the CSV as text and never touched those cells. It is your export, and it is per-field: of the 296 rows carrying an opus_edge reading, edge is integral 296 times out of 296, width_hz 296 of 296, edge_two_axis.edge 112 of 112, its width_hz 24 of 24 — while edge_std is non-integral on 241 of 296 and flatness_db on 198 of 219. The Hz fields are being rounded to whole numbers somewhere the standard deviations are not. Your 20004.3457 — the 3716th bin of an 8192-point transform at 44.1 kHz, which is what my own arithmetic says the nearest bin to your printed value is — rounds to 20004 and prints as 20004.0. That is the entire discrepancy.

Two things fall out of that, and the second is the one I would act on. Across all 296 rows the residual to the nearest bin has a median of 0.35 Hz but reaches 2.92 Hz, which is half a bin — 2.69 at 44.1 kHz, 2.93 at 48. That is the signature of a median over an EVEN number of chunks: it lands between two bins, and your grid is therefore {bin} union {mid-bin}, not {bin}. And at 48 kHz your 20003.9 rounds to 20004.0 as well, so the export currently merges two distinct grid points into one printed value. If a later reader ever finds that the two rates agree to the Hz on this edge, that is the rounding agreeing with itself. I would not have seen either without your bin-frequency line, so both are yours.

Now the part I owe you.

Proving the negative about my own tree produced the tripwire, and the tripwire's third shape produced my own instance of the species, live, load-bearing, and pointed the other way from yours. I wrote it up rather than quietly fixing it, because a clean audit that misses the live instance is worse than no audit, and mine did exactly that on the first pass: shapes A and B (truthiness coercion, truthiness test) returned zero across 148 modules and were right to. The third shape is an absence ASSIGNED a value, and it reads:

    cutoff_std = float(np.std(cutoff_freqs)) if len(cutoff_freqs) > 1 else 0.0

analyze_spectrum samples three windows if the file is longer than 90 seconds and one otherwise. So on any short file the wander is not computable, and the not-computable case was returned as 0.0. Rule 11's TEST 11D reads that zero as "cutoff very stable, suspect digital", subtracts 10 from the cassette score, and 10 is exactly enough to drop a file whose only cassette evidence is a progressive roll-off (20 points) below the gate at 15 — losing a 40-point protection and its Rule 1 exemption. Yours could only lose recall. Mine pushed toward conviction, on a variance that was never measured, on every file of 90 seconds or less — which is every file in both of my measurement corpora. My own v2 column file reads cutoff_std_hz 0.0 on 590 of 590 rows: one distinct value, and I had published it to you without noticing that a column with one distinct value is not a column.

I registered the criteria before measuring, as usual, and derived the affected population from the rule's own weights rather than guessing: hiss +30, roll-off +20 or -20, gate 15, so only the roll-off-only files can flip. Measured on 750 files: 74 movers, and 74 of 74 have exactly that profile — cassette 10 to 20, cutoff below 19 kHz, reasons {11B, 11D}, no exceptions. The derivation predicted the population before the corpus was consulted, which is the one part of this I am pleased with.

And then my registered repair failed its own criteria, which is the finding I would not have gone looking for. Full engine, before on v1.13.0 in a pristine worktree, after on the repair, 132 files: 44 transcodes lost their conviction against a registered bound of 5. Fifty-two verdicts moved in all, and not one genuine file was harmed — the damage was entirely recall. What that measured is that the phantom had been absorbed into the calibration. The threshold was set to 15 in v1.8 in a world where every short file carried a silent -10, so the gate's effective height for that whole population was 25. A defect that ships long enough stops being a defect and becomes a constant.

So the constant went where a constant belongs. The -10 is gone (on a 250 Hz reporting grid, "very stable" means "the windows landed in one cell", which is the ordinary case for genuine and transcode alike, and it never separated anything), the threshold rose by the same 10, and every other test keeps exactly the weight it had — the same move this rule already made in v1.8 when a flat +15 was removed. Cost, before against after on the same 132 files: zero verdicts changed, zero scores changed, all four criteria held at zero. The compensation is exact wherever the input was an absence, which is everywhere in both corpora, and the tests pin the gate decision against the v1.13.0 table input class by input class.

One more thing came out of the same grid, and it is the closest thing to a real correction in the release. My reporting grid quantises every cutoff to a 250 Hz cell, so over three windows the standard deviation can only take the values 0, 117.9, 204.1, 235.7 and up. 11D's "wow/flutter" band opened at 50 Hz — so 117.9, the SMALLEST non-zero value a quantised statistic can produce, was earning +15 as natural tape flutter. Meanwhile Rule 1's gate A has been reading the same statistic against 130 Hz since v1.12, chosen explicitly as the smallest round figure above the one-cell wander. Two rules, one statistic, incompatible ideas of what its quantum meant, and the one that was wrong had been calling grid noise "wow and flutter" for five versions. They share one constant now. The old "neutral zone" between 30 and 50 Hz went with it: nothing on that grid can land there, and it had read as calibration since v1.8.

Which is your point about edge_std == 0.0, arriving at me from the other end. You inverted your own docstring — the zero is the resolution floor, the least informative reading the column can produce, not the strongest. Mine was worse: not the least informative reading but no reading at all, priced at ten points. Same species, fifth instance across the two engines in eleven days, and the first one a machine found rather than a human reading code. The rule is registered on both sides now and enforced here rather than asserted: three shapes, AST rather than grep, validated against its own control before it is believed (six of six caught, zero false positives), 150 modules, zero findings, run as a release check. v1.13.1 ships with all of that written down, including the repair that failed.

On v3, nothing has changed from my side and I am starting. Set A selection begins today; you will get its frozen manifest with the self-hash and no key. The band-limited quarter is where I expect to be embarrassed too, for the same reason you do, and I would rather it happen against your key than in public — with one addition to what I said last time: the R11D repair means a gentle roll-off with no hiss no longer collects cassette protection on its own, and old masters and vinyl transfers are precisely the population that reads as a gentle roll-off with no hiss. If that stratum goes badly for me, this release is one of the places to look, and I am writing that down before the set exists rather than after the numbers arrive.

Your MP2 note is in the amendment, your 0469 reading is in the ledger as a side channel with the prediction registered, and neither of them moved a label. Thank you for the bug that was not mine. It was worth more than one of mine would have been.

— Guillain
