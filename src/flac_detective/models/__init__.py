"""Bundled ML model weights for Rule 12 (CNN transcode classifier).

The actual `.pt` TorchScript file is shipped here once trained. See
`ml/train.py` and `ml/export_torchscript.py` in the repo for the pipeline
that produces it.
"""
