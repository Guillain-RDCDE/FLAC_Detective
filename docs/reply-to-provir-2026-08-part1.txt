Jamie —

The long one, in the order it happened — and the last item is the one I would have led with if the order were not the right order: your version lock was the whole of my W4.

The elec-at-128 deviation first, because you asked two precise questions and both have precise answers now. The library is not libfdk-aac: Ubuntu's package is built from the source package fdk-aac-free — the Debian fork with the patented SBR/PS parts removed, AAC-LC core — at 2.0.2-3~ubuntu4, libfdk-aac.so.2 sha 25f9b1e110adc14a…, front-end fdkaac 1.0.0-1build1. That is candidate one, and it would explain why three cells match your four Windows builds bit for bit and one does not: the fork touches what it touches. The front-end prints no effective settings, so I measured them the only way that is honest — by decoded-PCM identity against the shipped elec_128, since the m4a container carries a timestamp in mvhd and its hash means nothing. Afterburner: on by default (-a 1 reproduces the shipped stream exactly, -a 0 does not). Bandwidth: 15,500 Hz effective at 128 CBR LC stereo — I stepped 13,000 to 17,000 in 100 Hz and exactly one value reproduces the stream. And one thing observed, not explained: an explicit -p 2 produces a different stream from the default that the help text calls "2 (default)". The variants are in the FDK folder at the same link, a subfolder elec128_variants with its own NOTE: bw15500 (which is the shipped stream made explicit), afterburner0, profile2, hashes inside. The bisection I would run from your side: encode elec at 128 with your builds at -w 15500 -a 1 explicitly. If they then match mine, the defaults moved between fdkaac 1.0.0 and 1.0.3+; if they still differ, it is the fork.

ARM-2, scored with the key open — and the registration's own premise failed before anything else did, which I keep rather than fix quietly. I wrote "converted = run-1 rate 48000" and counted 147; your 179 is 147 at 48 kHz plus 30 at 96 kHz plus two your first run read as 44000. Converted means every rate other than 44.1, and on that criterion A2-1 holds: zero movers among the 420 byte-copied files, all eight on converted items, your engine deterministic on identical bytes. The two predictions I would have bet on both failed. I predicted the leak's footprint concentrated on Opus — at least five of eight movers — and measured three: the eight are three opus_256, three genuine (two of them the 96 kHz recordings), one aac_ff256, one mp3_320. And I predicted the movement toward clear, because removing a tell that helped you catch Opus should cost Opus recall — and two Opus items went the other way, flagged to convicted, after the downsample. Per label your detection is essentially unchanged: opus 60 of 60 to 59, aac_ff256 60 to 59, genuine flagged 17 to 18. The sentence that survives: the 48 kHz rate never carried your Opus recall — you read Opus at 60 of 60 with the leak and without it — which makes the v2 set's rate equalisation hygiene rather than rescue, and makes my disclosure of that leak in August a correct thing to have done for a smaller reason than I thought. The eight, since you are writing about them: 0080 aac_ff256 flagged→clear, 0234 genuine flagged→clear (96 kHz), 0253 opus flagged→convicted, 0256 genuine clear→flagged (96 kHz), 0362 opus flagged→convicted, 0449 opus flagged→clear, 0505 mp3_320 convicted→flagged, 0508 genuine clear→flagged (48 kHz). Net: three toward correct, three away, one detection-preserving downgrade, on 179 converted files — 4.5 percent.

Now the era battery, registered as its own campaign as you asked, in shareable form for your mirror — ml/exchange/ERA_BATTERY_REGISTRATION_2026-08-22.md in the repository, the same text as the script's sealed docstring. Seven probes from your ladder, period builds: 3.90.3, 3.92 (the exhibit key), 3.93.1, 3.96.1, 3.97, 3.98.4, and lame.exe 3.100 — that last rung to isolate the route axis, since it is my shipped probe's generation through your route instead of ffmpeg's. One bent rung, bent on your measurement: the period 3.97 binary will not launch on this Windows (WinError 216), so your 2026 MinGW rebuild, which agrees with it to the decimal on your bench, stands in. Each read is two roundtrips through that lame.exe at CBR 320, files never pipes, output judged by size and decodability, the minimum over the canonical phases, the full 576 search under 3.92 on the wilds; every bar cut on the probe's own twenty genuine reads, raw R never compared across probes — the E-series lesson. Selftest first: a 3.92 transcode reads 7.41 under the 3.92 probe against 22.59 fresh. Four predictions.

EB2, the lock control, failed in both halves, and the failure is a shape rather than a contradiction: I predicted every era probe blind to my Lavc-3.100 lab arms and the lame.exe-3.100 probe sighted on them. Measured: 3.90.3, 3.93.1 and 3.98.4 are blind as predicted (8, 8, 7 of 8 above bar), but 3.92 reads four of the eight arms below its bar, 3.97 two, 3.96.1 one — and lame.exe 3.100 sees only four of eight of its own generation's arms, the route alone moving half of them out of reach. Your lock is real and graded, not binary: generation plus adjacent, with soft edges, and the route is an axis of its own weight.

EB3 is the result. Twenty-three of the 34 wilds read below some era probe's lawful bar at the best canonical phase. Under lame3.90.3 alone: twenty of 34, and not near the bar — at the fixed point. The twenty read between -0.55 and 0.82 dB where that probe's genuine minimum is 1.21, its genuine median 2.86, and my modern-route lab arms read 1.63 to 5.92, like genuine. Same 34 files that read 0 of 34 under the 3.100 probe at the best of all 576 phases two days ago. EB4 held at 100 percent: twenty of the 23 recovered files read best on 3.90.3, the other three on 3.97 — the discs' encoder generation is LAME 3.90.x, the 2002-2004 alt-preset era, read off the audio. Their best canonical phases split 529 fourteen times, 0 ten, 47 ten — the fourteen at 529 are untrimmed decodes by your rule.

So the sentence I stamped — "the mastering chain destroys the fixed point" — was wrong, and it is withdrawn by dated amendment in the three places it lived: the phase probe, the remaster arm, the W preregistration. The fixed point was never destroyed. My probe was two steps from the discs' encoder, generation and route, and the wilds sat at their own encoder's fixed point the whole time, at a phase the canonical three already reach. The remaster arm keeps what PS4 measured about itself — the limiter layer moves MY arm, at its right phase, to about 2 dB — and stops being a claim about the wild. The side-channel and temporal witnesses' collapse on those discs (the noise-floor layer) is untouched by any of this; those families read no fixed point. W4's explanation, measured: version lock plus route lock. Yours, whole, and I would not have found it without the binaries.

(1 of 2)
